# In-House Company technical test

## Primero lanzar la API de Backend

### Pasos para correr frontend:

- En el directorio de frontend correr `npm install`
- Correr `npm run dev`
- El servidor será abierto en http://localhost:5173/
